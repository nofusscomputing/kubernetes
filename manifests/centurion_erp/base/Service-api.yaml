---
apiVersion: v1
kind: Service
metadata:
  name: centurion
  labels:
    app.kubernetes.io/name: centurion
    app.kubernetes.io/part-of: centurion-erp
spec:
  selector:
    app.kubernetes.io/name: centurion
    app.kubernetes.io/part-of: centurion-erp
  ports:
    - name: api
      port: 8000
      targetPort: api
    - name: ui
      port: 80
      targetPort: ui
