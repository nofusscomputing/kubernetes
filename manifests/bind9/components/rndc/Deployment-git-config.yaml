---

apiVersion: apps/v1
kind: Deployment
metadata:
  name: bind
spec:
  template:

    spec:

      containers:

        - name: git

          volumeMounts:

            - name: data
              mountPath: /etc/bind/conf.d/extra
              subPath: extra/
